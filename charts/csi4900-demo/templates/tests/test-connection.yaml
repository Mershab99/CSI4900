apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csi4900-demo.fullname" . }}-test-connection"
  labels:
    {{- include "csi4900-demo.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csi4900-demo.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
