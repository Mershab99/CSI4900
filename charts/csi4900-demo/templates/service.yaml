apiVersion: v1
kind: Service
metadata:
  name: {{ include "csi4900-demo.fullname" . }}
  labels:
    {{- include "csi4900-demo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "csi4900-demo.selectorLabels" . | nindent 4 }}
